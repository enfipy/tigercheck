fn walk_implicit_call_related(
    tree: u32,
    source: u32,
    node: u32,
    file_path: u32,
    result: u32,
    visited: u32,
) void {
    _ = tree;
    _ = source;
    _ = node;
    _ = file_path;
    _ = result;
    _ = visited;
}

pub fn main() void {
    walk_implicit_call_related(1, 2, 3, 4, 5, 6);
}
